
Day 62 (2026/7/15)

今天的推进：

类型 A（功能）：无

类型 B（认知）：
- 完成 Decision 2 拒 A 书面版（单一 ask wrapper）：
  * 核心是 agent 退化为转发层——只递 server 给的答案，不做判断
  * 引出"沉默失败"概念：server 检索漏了内容，agent 和用户都无从察觉
  * 落地例子：用户问 BC PNP 走不通的替代路径，文档里的 AIP 段落
    未被字面命中就被丢弃
- 完成 Decision 2 拒 C 书面版（纯 retrieve 让 agent 自己 reason）：
  * 核心是 agent 端通用 LLM 不掌握知识库的元知识——库里几个 AIP、
    BC PNP 项目怎么命名、术语哪一版，它只能猜
  * 枚举把结构知识封装在 server，歧义在 schema 层消除（aip_atlantic
    vs aip_agrifood），不靠模型推断
  * 变更成本：C 让知识库结构泄漏进 agent prompt，形成耦合；B 只改
    server，agent 面对稳定 tool schema
- 完成拒 A + 拒 C 的口头合并版（面试双头攻击："为什么不合并 / 
  为什么不拆散"），一次答完两个方向

今天真正多出来的抽象工具：
- "封装光谱"——拒 A 和拒 C 不是两个孤立的理由，是同一根轴上的
  两个端点。这根轴是封装的深浅：
  * 往上 = 单一 ask，把判断全收进 server，代价是 agent 退化、
    server 一漏无人察觉
  * 往下 = 纯 retrieve，把判断全推给 agent，代价是它得猜知识库结构
  * 选 B 不是折中，是给这根轴定了一个边界：知识库结构信息归 server
    （所以不往下），最终推理归 agent（所以不往上）
- 以前只会孤立地说"这个方案不好"；现在能说清一个设计在光谱上的
  位置，以及边界为什么划在这里

类型 C（战略）：
- 今日完成度高于开始时目标（口头合并版超出计划），主动砍掉总结段
  和 Decision 3，避免稀释手感——总结段留待睡眠固化后明天写

对 8/1 目标的贡献：
- Decision 2 三种面试问法已备齐两种半（querymap 层 + 拒 A + 拒 C），
  且掌握了双头追问的应对结构
- "封装光谱"是可迁移的架构判断框架，不只用于这一个 decision